---
- name: Enable and start services (common)
  ansible.builtin.service:
    name: "{{ item }}"
    state: started
    enabled: true
  loop: "{{ common_services | default([]) }}"
  when: (common_services | default([])) | length > 0
...
